numbers_list = [1, 3, 5, 6, 8, 4]

even_numbers_list = []
for num in numbers_list:
    is_even_num = num % 2 == 0
    if is_even_num == True:
        even_numbers_list.append(num)

result = sum(even_numbers_list)
print(result)